import { Button, Container } from "./styled";

const Buttons = () => {

	return (
		<Container>
			<Button>
				change view to list
			</Button>
			<Button>
				show more info
			</Button>
		</Container>
	);
};

export default Buttons;